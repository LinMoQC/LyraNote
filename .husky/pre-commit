npx lint-staged

# Run API tests if Python venv is available
if [ -f "apps/api/.venv/bin/pytest" ]; then
  echo "Running API tests..."
  cd apps/api && .venv/bin/pytest tests/ -q --tb=short
  cd ../..
else
  echo "Skipping API tests (apps/api/.venv not found — run './lyra dev' to set up)"
fi
